zuul:
  routes:
    myConfig:
      path: /myConfig/**
      serviceId: config
      #不过滤cookie信息
      sensitiveHeaders:
#简洁写法
#    config: /myProduct/**
  #排除路由
  ignored-patterns:
    - /**/product/listForOrder
