zuul:
  #不过滤cookie信息
  sensitive-headers: 
  routes:
    one:
      path: /myProduct/**
      serviceId: product
#简洁写法
#     config: /myConfig/**
#     product: /myProduct/**
  #排除路由
  ignored-patterns:
    - /**/product/listForOrder
